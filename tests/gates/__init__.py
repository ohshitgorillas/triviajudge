"""The gate tests, a package so that this directory's ``conftest`` has a name of its own.

``tests`` is on the type checker's path, so a second ``conftest.py`` under it
would otherwise claim the same module name as the suite's own and neither could
be checked.
"""
